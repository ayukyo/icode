# ===========================================================================
# 本仓开启"每阶段自动 commit + push"，没有人工把关机会，
# 因此本文件是防泄漏的第一道闸门：宁可多过滤，不可漏。
# 任何新增产物类型，先来这里登记，再允许它出现在工作区。
# ===========================================================================

# ---------------------------------------------------------------------------
# 密钥与凭据（最高优先级，多形态兜底）
# ---------------------------------------------------------------------------
.env
.env.*
*.env
*.key
*.pem
*.p12
*.pfx
*.secret
*.secrets
*KEY*.txt
*key*.txt
*Key*.txt
*密钥*.txt
*密码*.txt
*token*.txt
*credential*
secrets/
.secrets/
credentials.json

# 本地（未入库）配置：可指向仓外密钥文件路径
icode.local.toml
icode.local.json
*.local.toml
icode.local/

# ---------------------------------------------------------------------------
# Agent / ICODE 运行产物（与 icode-skill 的约定一致）
# ---------------------------------------------------------------------------
.icode_output/
.ico_*/
*.icontrol.lock
*.ico_events.jsonl
*.ico_metadata.json

# ---------------------------------------------------------------------------
# Agent 工作数据：本地记忆不入库；项目级技能（skills/）保留共享
# ---------------------------------------------------------------------------
.workbuddy/*
!.workbuddy/skills/
!.workbuddy/skills/**

# ---------------------------------------------------------------------------
# Python 构建与缓存
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
wheels/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---------------------------------------------------------------------------
# IDE 与编辑器
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo

# ---------------------------------------------------------------------------
# 操作系统
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# 临时与日志
# ---------------------------------------------------------------------------
*.log
*.tmp
.tmp/
tmp/
*.bak
*~

# ---------------------------------------------------------------------------
# 二进制 / 大文件产物（构建输出，不应入库）
# ---------------------------------------------------------------------------
*.whl
*.tar.gz
*.zip
*.exe
*.dll
*.so
*.dylib

# ---------------------------------------------------------------------------
# 注意：以下内容**刻意不过滤**（需要入库）
#   - vendor/icode-skill      子模块以 gitlink 形式入库（不提交其内容）
#   - docs/                   方案与决策文档
#   - tests/fixtures/         E2E 靶场
#   - .gitignore/.gitattributes 仓库治理文件本身
# ---------------------------------------------------------------------------
